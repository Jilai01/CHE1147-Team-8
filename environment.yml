name: CHE1147 Team 8
channels:
  - conda-forge
  - defaults
dependencies:
  # Core Python
  - python=3.10
  - pip
  - jupyterlab
  - notebook<7.0.0
  - nb_conda

  # Data Science stack
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - seaborn
  - matplotlib
  - plotly
  - bokeh

  # pip-only packages (if any specific versions are missing from conda)
  - pip:
      - kaggle
